import { Box } from '@mui/material'

function Spinner() {
  return (
    <Box
      sx={{
        margin: 'auto',
        marginTop: '40%',
      }}
      className="loader"
    ></Box>
  )
}

export default Spinner
